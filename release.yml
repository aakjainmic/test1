trigger:
      branches:
        include:
         - refs/tags/R1.0.0
        exclude:
         - '*'
resources:
  pipelines:
  - pipeline: test2
    source: test2


steps:
- script: |
    echo $(resources.pipeline.myresourcevars.pipelineID)
